# Index knowledge base

Use the `index_docs` MCP tool to index a local folder into ChromaDB.

Arguments: $ARGUMENTS

Format: `<folder-path> [collection-name]`

Example: `/path/to/runbooks my-runbooks`

Confirm how many documents were indexed and the collection name.
