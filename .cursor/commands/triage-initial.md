# Live oc triage: Initial

Use the `debug_openshift_cluster` MCP tool with issue:

"Run initial triage: cluster version, all cluster operators, nodes, and non-Running pods"

Additional context: $ARGUMENTS

Run live `oc` commands and summarize findings. Match web UI workflow **initial_triage**.
