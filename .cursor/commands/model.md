# Set LLM model

Use the `switch_llm_mode` MCP tool — keep current mode, only change the model.

New model: $ARGUMENTS

Examples: `composer-2.5`, `llama3.3:latest`, `claude-sonnet-4-5@20250929`

Confirm the updated model after switching.
