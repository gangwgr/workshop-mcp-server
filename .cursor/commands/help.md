# List all commands

Show the user every available slash command for this MCP workshop project:

**Web UI chat:** `/help`, `/status`, `/mode`, `/model`, `/debug`, `/mustgather`, `/ask-kb`, `/list-kb`, `/index-kb`, `/preset`, `/script`, `/triage`, `/analyze`, `/clear`

**Cursor IDE:** `/debug-cluster`, `/analyze-mustgather`, `/ask-knowledge-base`, `/index-knowledge-base`, `/list-knowledge-base`, `/mode`, `/status`, `/preset-health-check`, `/preset-degraded-cluster`, `/preset-network-issues`, `/analyze-etcd`, `/triage-initial`, `/triage-apiserver`, `/triage-pod-crashloop`, `/switch-llm`

**MCP tools:** `debug_openshift_cluster`, `analyze_mustgather_bundle`, `ask_docs`, `index_docs`, `switch_llm_mode`

Briefly explain what each category does.
