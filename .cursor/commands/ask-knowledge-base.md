# Ask knowledge base

Use the `ask_docs` MCP tool to search indexed runbooks and documentation.

Question: $ARGUMENTS

Return a concise answer with source references from the knowledge base collections.
