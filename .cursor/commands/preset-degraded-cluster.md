# Must-Gather preset: Degraded Cluster

Use `analyze_mustgather_bundle` on the bundle path, focusing on:
- Degraded cluster operators
- Problem pods
- Warning events
- etcd health

Bundle path or URL: $ARGUMENTS

Match the web UI **Degraded Cluster** preset workflow.
