# Live oc triage: Pod CrashLoop

Use the `debug_openshift_cluster` MCP tool with issue:

"Pod CrashLoop / Not Ready — describe pod, get previous logs, check events"

Pod/namespace details: $ARGUMENTS

If namespace/pod not provided, ask the user. Match web UI workflow **pod_crashloop**.
