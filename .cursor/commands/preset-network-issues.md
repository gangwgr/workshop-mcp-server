# Must-Gather preset: Network Issues

Use `analyze_mustgather_bundle` on the bundle path, focusing on:
- Network / OVN diagnostics
- OVN database health
- Pods in openshift-ovn-kubernetes namespace

Bundle path or URL: $ARGUMENTS

Match the web UI **Network Issues** preset workflow.
