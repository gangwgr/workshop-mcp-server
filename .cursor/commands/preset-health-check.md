# Must-Gather preset: Quick Health Check

Use `analyze_mustgather_bundle` on the bundle path, focusing on:
- Cluster version
- Cluster operators (degraded/unavailable)
- Pods with problems
- Node readiness

Bundle path or URL: $ARGUMENTS

If no path given, ask the user. Run focused analysis like the web UI **Quick Health Check** preset.
