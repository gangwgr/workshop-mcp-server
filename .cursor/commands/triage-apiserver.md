# Live oc triage: API Server

Use the `debug_openshift_cluster` MCP tool with issue:

"API server health check: operator status, kube-apiserver pods, /healthz /livez /readyz endpoints"

Additional context: $ARGUMENTS

Match web UI workflow **apiserver_health**.
