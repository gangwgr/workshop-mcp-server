# Switch LLM mode

Use the `switch_llm_mode` MCP tool to change the active LLM backend.

Requested mode/model: $ARGUMENTS

Supported modes: `ollama`, `claude`, `cursor`, `template`

Examples:
- `cursor composer-2.5`
- `ollama llama3.3:latest`
- `claude`

Confirm the new mode after switching.
