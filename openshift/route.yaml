apiVersion: route.openshift.io/v1
kind: Route
metadata:
  annotations:
    haproxy.router.openshift.io/timeout: 180s
  labels:
    shard: internal
  name: workshop-mcp-server
  namespace: ddis-asteroid--template
spec:
  host: workshop-mcp-server.apps.int.spoke.preprod.us-west-2.aws.paas.redhat.com
  pathType: Prefix
  to:
    kind: Service
    name: workshop-mcp-server
  tls:
    termination: reencrypt
  port:
    targetPort: https
