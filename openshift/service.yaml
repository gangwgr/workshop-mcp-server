kind: Service
apiVersion: v1
metadata:
  name: workshop-mcp-server
  namespace: ddis-asteroid--template
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: workshop-mcp-server-tls
spec:
  ports:
    - name: https
      protocol: TCP
      port: 8443
      targetPort: 8443
  selector:
    app: workshop-mcp-server
